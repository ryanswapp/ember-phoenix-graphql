<div class="container">
  <div class="page-header">
    <h1>Posts</h1>
  </div>
  <div class="list-group">
    {{#each model as |post|}}
      <div class="list-group-item">
        <h4>{{post.title}}</h4>
        <p>{{post.content}}</p>
      </div>
    {{/each}}
  </div>
  {{outlet}}
</div>
